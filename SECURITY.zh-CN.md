# 安全边界

[English](SECURITY.md) · 简体中文

**这是远程终端，不是文件夹沙箱。** `projectRoot` 只约束展示目录。终端、shell 和 Codex 沿用原 Linux 用户的文件与网络权限。生产入口拒绝 root，但这不是系统隔离机制。

## 已有保护

- 单实例、单批准身份：GitHub 数字 ID，或明确配置的密码＋TOTP；失败限流，不自动降级。
- 精确 Host / Origin 校验，HTTPS、HttpOnly Cookie，WebSocket 也校验身份和来源。
- 预览独立来源、短期授权、iframe sandbox；静态路径边界与符号链接检查。
- 端口预览默认关闭；本地管理 socket 仅文件所有者可用。
- 默认只读、输入租约、同名 tmux 重建校验；不确定的输入不自动重放。
- 运行资料与源码分开；仓库排除凭据、真实配置、历史、日志和浏览器档案。

## 不承诺的能力

没有多租户 RBAC、SSO、完整审计录像、自动数据留存清理或高可用集群。状态提示依赖 TUI 线索；回执不是分布式 exactly-once 保证。未经真实部署和客户端测试，不能保证某条公网路线稳定。

生成的 HTML 可以执行 JavaScript，独立来源不代表内容可信。只展示专用目录，不暴露项目根目录和秘密。端口预览的 WebSocket 可能支持写入，不能批准管理端口。目录过滤和文本脱敏均不能识别所有敏感信息。

## 部署与数据

每个使用者采用独立非 root Linux 用户、tmux socket、Codex 环境和实例。不要赋予无关 sudo、Docker socket 或共享秘密权限；不互信场景需要额外 VM/容器与网络隔离，不能仅靠不同端口。

Node 只监听回环。实例目录 0700、凭据 0600。`runtime/` 的回执可能含消息正文，应当作为敏感资料加密备份；原 Codex 历史另行管理。GitHub token 不向前端或 AI 注入。站内登录为内存状态，服务重启即失效。

撤权要停止实例/隧道或修改身份并重启，不能只更改 DNS。TLS、上游代理日志、主机补丁、依赖审查与真实客户端验收见 [部署清单](docs/DEPLOYMENT.md)。

## 提交与报告

提交前运行 `npm run lint`，再人工检查 `git diff --cached`。扫描器只是辅助，不保证不存在秘密；截图只能用演示数据，禁止真实用户记录。不要强制添加 `.gitignore` 排除的内容。

报告漏洞时提供不含凭据的最小复现，使用仓库的私密漏洞报告渠道（若已启用），不要在公开 Issue 贴 Cookie、密钥或带 code/state/ticket 的 URL。只测试自己的隔离实例。
