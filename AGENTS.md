# 本项目协作规则

- 这是 Codex 专用远程工作台。聊天、执行记录和成果预览是中心；tmux 与终端是承载和辅助层。保持聚焦，不扩成通用运维平台。不要读取或复制其他部署的凭据或真实聊天历史。
- 源码与测试可本地修改；DNS、服务器、OAuth、systemd/Nginx部署和公网开放均需明确授权，不因“写文档”而执行。
- 只在测试新建的socket和会话验证输入；禁止向真实tmux注入测试、自动回答审批或kill-server。
- 默认非root、一人一实例；预览目录限制不是终端权限沙箱。不要把未实现的SSO/RBAC/审计写成已具备。
- 优先复用统一鉴权、Registry、终端执行与预览模块。根因未明确时不堆重试和特殊规则。
- 产物/缓存使用本项目runtime，设置TMPDIR/XDG缓存，不修改HOME/CODEX_HOME；部署目标使用明确指定的目录。
- 展示目录只放可展示资源。HTML/媒体必须手机可用，媒体推荐H.264 MP4，资源使用相对路径。
- 保留 MIT 许可和第三方声明。不自动 push，不提交真实 config、runtime、node_modules 或秘密。
